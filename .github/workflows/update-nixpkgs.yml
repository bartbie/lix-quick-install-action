# This file is automatically generated from Nix configuration. Do not edit directly.

concurrency:
  cancel-in-progress: false
  group: update-nixpkgs
jobs:
  gate:
    if: needs.prepare.outputs.changed == 'true'
    needs: prepare
    secrets: inherit
    uses: ./.github/workflows/cicd.yml
    with:
      ref: auto/update-nixpkgs
  merge:
    if: needs.prepare.outputs.changed == 'true'
    needs:
      - prepare
      - gate
    permissions:
      contents: write
    runs-on: ubuntu-24.04
    steps:
      - uses: step-security/harden-runner@9af89fc71515a100421586dfdb3dc9c984fbf411
        with:
          egress-policy: audit
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd
        with:
          fetch-depth: 0
          ref: main
      - name: Fast-forward main to auto/update-nixpkgs
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git fetch origin auto/update-nixpkgs:auto/update-nixpkgs
          git merge --ff-only auto/update-nixpkgs
          git push origin main
          git push origin --delete auto/update-nixpkgs
    timeout-minutes: 60
  prepare:
    outputs:
      changed: ${{ steps.diff.outputs.changed }}
    permissions:
      contents: write
    runs-on: ubuntu-24.04
    steps:
      - uses: step-security/harden-runner@9af89fc71515a100421586dfdb3dc9c984fbf411
        with:
          egress-policy: audit
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd
        with: {}
      - uses: canidae-solutions/lix-quick-install-action@cdb5d0931be2223edbb9bad884341fcacdac298f
        with:
          lix_archives_url: https://github.com/canidae-solutions/lix-quick-install-action/releases/download/v4.0.0
          lix_version: 2.94.0
      - name: Update nixpkgs
        run: nix-shell ./shell.nix --run 'npins update nixpkgs'
      - id: diff
        name: Check for changes
        run: |
          if git diff --quiet npins/sources.json; then
            echo "changed=false" >> "$GITHUB_OUTPUT"
          else
            echo "changed=true" >> "$GITHUB_OUTPUT"
          fi
      - if: steps.diff.outputs.changed == 'true'
        name: Regenerate repo files
        run: nix-shell ./shell.nix --run write-repo
      - if: steps.diff.outputs.changed == 'true'
        name: Bump release version
        run: nix-shell ./shell.nix --run bump-release-minor
      - if: steps.diff.outputs.changed == 'true'
        name: Push to auto/update-nixpkgs
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git checkout -B auto/update-nixpkgs
          git add -A
          git commit -m 'chore: bump nixpkgs'
          git push --force origin auto/update-nixpkgs
    timeout-minutes: 60
name: Update nixpkgs
"on":
  schedule:
    - cron: 0 6 * * 1
  workflow_dispatch: {}
permissions:
  contents: read
